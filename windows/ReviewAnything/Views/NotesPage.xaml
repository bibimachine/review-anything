<Page x:Class="ReviewAnything.Views.NotesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:ReviewAnything.ViewModels"
      Title="NotesPage">
    <Page.DataContext>
        <vm:NotesViewModel />
    </Page.DataContext>
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="笔记管理" FontSize="24" FontWeight="Bold" Margin="0,0,0,16"/>

        <!-- 板块选择 -->
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Sections}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding}" Padding="12,6" Margin="4"
                            Command="{Binding DataContext.SelectSectionCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                            CommandParameter="{Binding}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- 笔记列表 -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Notes}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  Margin="0,16,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="文件名" Binding="{Binding FileName}" Width="*"/>
                <DataGridTextColumn Header="内容预览" Binding="{Binding Content}" Width="2*"/>
                <DataGridTemplateColumn Header="操作" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="删除" Padding="8,4"
                                    Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                    CommandParameter="{Binding Id}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
